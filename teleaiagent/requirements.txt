# Telegram Bot (AsyncIO)
aiogram==3.13.0

# Database (AsyncIO SQLite)
aiosqlite==0.20.0

# Text Processing
markdown2==2.4.10

# Async Web Requests
aiohttp==3.10.10
aiofiles~=23.2.1

# Image Processing
Pillow==10.4.0

# Vector Database
qdrant-client==1.11.0
# Note: PyTorch CPU-only and sentence-transformers are installed separately in Dockerfile
# Architecture-specific installation:
# x86_64: torch==2.4.0+cpu (optimized builds from PyTorch wheel index)
# ARM64:  torch==2.4.0 (standard PyPI builds for ARM64 compatibility)

# System Monitoring
psutil==6.0.0

# Timezone Handling
pytz==2024.2
